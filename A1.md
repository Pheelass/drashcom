````
import { createClient } from 'redis';
import { TokenBucket } from '@redis/token-bucket'; // erro aqui
const client = createClient({ url: 'redis://localhost:6379' });
await client.connect();

const limiter = new TokenBucket({
    redisClient: client,
    capacity: 10,        
    refillRate: 1,       
    refillInterval: 1.0  
});

	const { allowed, remaining } = await limiter.allow('user:123');
	if (allowed) {
	    console.log(`Request allowed. ${remaining} tokens remaining.`);
	} else {
	    console.log('Request denied. Rate limit exceeded.');
	    res.status(429).send({success: false, error:{message: "Too many request"}})
	    await client.disconnect();
	    return 0
	}
```

> o código deu errado no arquivo 'app.js' então é preciso ver como ele verdadeiramente funciona